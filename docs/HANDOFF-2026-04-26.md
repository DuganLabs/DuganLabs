# Handoff — 2026-04-26 autonomous session

This is the state at the end of a multi-hour autonomous session covering rebrand → BaseNative expansion → org infra → audit + automation.

## What's live

### t4bs (production at [t4bs.com](https://t4bs.com))
- **Rebrand complete.** Public name "Tabs" (visual mark T4BS = Time For BS, by The Synonym Toast Bunch). Tagline: "Pick a category. Solve the hidden phrase."
- **Lighthouse 100/100/100/100** on `/` (mobile, prod). Robots.txt + sitemap.xml + JSON-LD + Permissions-Policy headers all served from `public/`.
- **axe-core 0 violations** across lobby/play/submit/help/auth.
- **Share flow overhauled:** `/s/{id}` redirects to `/?play=N`; one OG card on share (no duplicate-link bug); pip-based icons (brand orange — non-Wordle).
- **Real moderator role**, ADMIN_HANDLES seeds at login. Mod queue + admin promotion UI.
- **Repo is now PUBLIC.**
- Adopted `@basenative/eslint-config` + `@basenative/tsconfig` via `link:` paths (will swap to semver once published).
- Lighthouse + bundle-size workflows wired against `DuganLabs/.github@v2` reusable workflows.
- 10 PRD-mapped issues at [t4bs/issues](https://github.com/DuganLabs/t4bs/issues), milestones M0 (closed) → M4 (open).

### BaseNative (open-source, `DuganLabs/basenative`)
- **30+ packages on main**, including 14 new ones from this session:
  `og-image, keyboard, auth-webauthn, admin, persist, share, wrangler-preset, doppler, favicon, combobox, claude-config, eslint-config, tsconfig` + overhauled `cli`.
- Versions bumped (PR #36 → #42 merged):
  - 1.0.0: keyboard, auth-webauthn, admin, persist, share, favicon, combobox
  - 0.4.0: cli
  - 0.2.0: og-image, wrangler-preset, doppler, claude-config, eslint-config, tsconfig
- Bundle budgets enforced: every new package under target. Total test count: **~1855 passing**.
- `.changeset/` consumed; CHANGELOG.md per package populated.
- `.npmrc` + workflow registry-url configured for **GitHub Packages**.

### DuganLabs/.github (org reusable workflows, public)
- **v2 tagged.** Workflows: `cf-deploy`, `d1-migrate`, `ci`, `lighthouse`, `bundle-size`, `labeler`, `auto-close-milestone`, `stale-state-sweep`.
- Issue templates (feature/bug) + PR template + profile README.
- Org-wide automation runs from this repo.

### duganlabs (org meta repo)
- **5 ADRs** in `docs/adr/`: BaseNative source of truth, .github source of truth, licensing, Doppler, GitHub-as-truth.
- Audit at `docs/AUDIT-2026-04-26.md` (greenput/warrendugan/warren-sys/ralph-station).
- PRD template + setup runbooks (project board, reusable workflow access).

### PendingBusiness (private)
- **PR [#5](https://github.com/DuganLabs/PendingBusiness/pull/5)** — auth swap to `@basenative/auth-webauthn` (passkeys). Migration `0004_webauthn.sql`. **Apply migration to prod D1 before merging.**
- **PR [#6](https://github.com/DuganLabs/PendingBusiness/pull/6) (Draft, stacked on #5)** — credential mgmt UI, 401→/login redirect, multi-tenant onboarding scaffold. 27 tests.

### Org infrastructure (applied to ALL 9 repos)
- **Labels** for `labeler.yml` (frontend, backend, infra, db, docs, deps, basenative, security, etc.)
- **Dependabot** weekly (npm grouped: @basenative/*, dev-deps; github-actions)
- **CODEOWNERS** (`* @warrendugan`)
- **CodeQL** workflow on public repos (t4bs, basenative, .github)
- **LICENSE** (Apache-2.0) + **SECURITY.md** on public repos

### Issues filed (audit-driven)
- [ralph-station#1](https://github.com/DuganLabs/ralph-station/issues/1) — IDE server hardening (security: high)
- [warrendugan#3](https://github.com/DuganLabs/warrendugan/issues/3) — broken deploys since 2026-04-19
- [warren-sys#6](https://github.com/DuganLabs/warren-sys/issues/6) — deprecate (duplicates warrendugan/apps/sys)?
- [GreenPut#26](https://github.com/DuganLabs/GreenPut/issues/26) — bump compat date + consolidate PRDs
- [duganlabs#8](https://github.com/DuganLabs/DuganLabs/issues/8) — **user-action items** (auth scopes, org settings)

### Dependabot triage
- **22 PRs auto-merged** (action version bumps): t4bs, basenative, duganlabs, warren-sys, greenput.
- **5 PRs annotated + held** for human review (npm major bumps: react 18→19, react-dom, satori 0.10→0.26, simplewebauthn 11→13×2).

## Things blocked on you (the human)

These are concentrated in [DuganLabs#8](https://github.com/DuganLabs/DuganLabs/issues/8). Top three by leverage:

1. **`gh auth refresh -s write:packages`** — unblocks publishing `@basenative/*` to GitHub Packages. Once published, t4bs and others can swap `link:` deps to real `^x.y.z` semver in `package.json`. The release workflow will work cleanly.

2. **Merge PendingBusiness PR #5 + apply migration 0004 to prod D1.** Auth swap is non-trivial — review the diff. Migration script: `cd ~/GitHub/DuganLabs/pendingbusiness && wrangler d1 migrations apply pendingbusiness-db --remote` BEFORE merging the PR.

3. **Org-level setting: "Allow GitHub Actions to create and approve pull requests"** — at https://github.com/organizations/DuganLabs/settings/actions. Unblocks the standard `changesets/action` flow so future releases don't need manual `changeset version` runs.

## Things that WILL keep working autonomously

- **Dependabot** runs weekly; safe action-version bumps will land on their own (security/infra category).
- **CodeQL** scans on public repos.
- **Lighthouse audit** runs on every t4bs PR (gates quality).
- **Stale issue sweep** runs Mondays.
- **Auto-close milestone** runs on every issue/PR close.

## What I'd do next session (priorities)

1. Once `write:packages` is available, publish all 14 new BN packages.
2. Wave 2: t4bs migrates to consume real `@basenative/*` semver (closes [t4bs#1-#7](https://github.com/DuganLabs/t4bs/issues)).
3. PendingBusiness PR #5 merge → PR #6 ready-for-review → merge.
4. ralph-station IDE security hardening (GH Issue #1) — tight scope, quick win.
5. warrendugan deploy fix.
6. PRD interactive build-out for greenput, pendingbusiness, warrendugan via `bn prd` + LLM session.
7. Org-level GitHub Project board (after `gh auth -s project` refresh).

## Inventory of new things (cross-repo)

| Repo | Branches/PRs | Recent milestones |
|---|---|---|
| t4bs | main (public) | rebrand + lighthouse 100s + a11y 0 + share flow + favicons + 10 issues + ADM/MOD + Doppler |
| basenative | main + 5 dependabot PRs (held) | 14 new packages + CLI overhaul + GH Packages config + bundle budgets + .d.ts complete |
| .github | main, v1 tag, v2 tag | 8 reusable workflows + issue/PR templates + profile + dependabot/CODEOWNERS/CodeQL |
| duganlabs | main | 5 ADRs + audit + handoff (this) + PRD template + 3 runbooks |
| pendingbusiness | feat/auth-webauthn (PR #5), feat/auth-followups (PR #6 Draft) | passkey migration + credential mgmt + onboarding scaffold |
| greenput | main + 1 dependabot PR (held) | infra + favicon + PRD stub + audit issue #26 |
| warrendugan | main + 1 dependabot PR (held) | infra + favicon + PRD stub + audit issue #3 |
| warren-sys | main + 0 deps | infra + favicon + PRD stub + audit issue #6 |
| ralph-station | main (private) | initialized + favicon + PRD stub + audit issue #1 |

## Commands you might run

```bash
# Check the user-action items
gh issue view 8 --repo DuganLabs/DuganLabs

# Refresh auth + publish
gh auth refresh -s write:packages
cd ~/GitHub/DuganLabs/basenative
echo "//npm.pkg.github.com/:_authToken=$(gh auth token)" > ~/.npmrc.tmp
NPM_CONFIG_USERCONFIG=~/.npmrc.tmp pnpm exec changeset publish

# Apply PB migration BEFORE merging PR #5
cd ~/GitHub/DuganLabs/pendingbusiness
wrangler d1 migrations apply pendingbusiness-db --remote
# then go merge PR #5 in the GH UI

# Review held dep major bumps
gh pr list --repo DuganLabs/t4bs --label wip
```
